export const config = {
  siteTitle: "FLUG",
  api: {
    url: "http://localhost:2000",
    uris: {
      users: "/users",
      blogs: "/blogs",
      events: "/events",
    },
  },
};

export default config;
