import React from "react";
import { Outlet } from "react-router-dom";

/**
 * Page Layout Component (Authentication)
 *
 * This component represents the layout of a page.
 */
export default function AuthLayout() {
  return <Outlet />;
}
